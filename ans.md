# 📋 LAB8: คำตอบที่ถูกต้องครบถ้วน (96 ข้อ)

## 📘 ส่วนที่ 1: ฝึก Trace โปรแกรม (ข้อ 1–66)
| ข้อ | Output ที่แสดงใน Console | จุดตรวจทาน / ข้อควรระวัง |
|:---:|:------------------------|:--------------------------|
| **1** | `Pass`<br>`End` | |
| **2** | `End` | |
| **3** | `Adult`<br>`Program End` | |
| **4** | `A`<br>`C` | *ไม่พิมพ์ B เพราะ 15 ไม่มากกว่า 20* |
| **5** | `A`<br>`B`<br>`C` | |
| **6** | `C` | |
| **7** | `A`<br>`D` | ❌ `(20 > 20) = false` จึงข้าม B ไปเลย |
| **8** | `A`<br>`B`<br>`D` | *(30 > 30) = false* ไม่พิมพ์ C |
| **9** | `A`<br>`B`<br>`C`<br>`D` | ทุกเงื่อนไขเป็นจริงทั้งหมด |
| **10** | `Fail`<br>`Done` | |
| **11** | `Pass`<br>`Done` | |
| **12** | `Pass`<br>`Done` | *ควรพิมพ์ทั้งสองบรรทัด* |
| **13** | `Even`<br>`Bye` | |
| **14** | `Odd`<br>`Bye` | |
| **15** | `Even`<br>`Bye` | *(12 % 2 != 0) = false* เข้า else |
| **16** | `Odd`<br>`Bye` | |
| **17** | `Grade A`<br>`Goodluck` | |
| **18** | `Grade C`<br>`Goodluck` | *(60 >= 80) false → (60 >= 70) false → else if ถูก* |
| **19** | `Grade F`<br>`Goodluck` | ผ่านทุกเงื่อนไข else if ไม่เข้าเลย |
| **20** | `Hot` | |
| **21** | `Cold` | |
| **22** | `Qualified`<br>`Bye` | |
| **23** | `Not Qualified`<br>`Bye` | |
| **24** | `Not Qualified`<br>`Bye` | |
| **25** | `Not Qualified`<br>`Bye` | |
| **26** | `Condition True`<br>`Bye` | ใช้ `||` ช่องแรกเป็นจริงก็ผ่านเลย |
| **27** | `Condition True`<br>`Bye` | `age >= 18` เป็นจริง แม้ score จะไม่ถึง |
| **28** | `Condition False`<br>`Bye` | ทั้งสองเงื่อนไขไม่ตรง จึงเข้า else |
| **29** | `A`<br>`C` | *(10 > 5 && 10 < 15) = false* เข้า else |
| **30** | `B`<br>`C` | 20 ไม่ได้อยู่ใน (5, 15) |
| **31** | `B`<br>`C` | 5 ไม่มากกว่า 5 *(x > 5)* เป็น false |
| **32** | `Adult`<br>`Pass`<br>`Bye` | if ซ้อนใน if ถูกเงื่อนไขทั้งคู่ |
| **33** | `Underage`<br>`Bye` | *(15 >= 18) = false* เข้า else ข้างนอก |
| **34** | `Adult`<br>`Fail`<br>`Bye` | age ตรง, score ไม่ผ่านเข้า else ใน if ซ้อน |
| **35** | `Positive`<br>`Even`<br>`Number` | |
| **36** | `Positive`<br>`Odd`<br>`Number` | |
| **37** | `Not Positive`<br>`Number` | (-5 > 0) = false เข้า else |
| **38** | `Grade A`<br>`Excellent Attendance`<br>`Finish` | if ซ้อนเช็ค attendance ต่อเนื่องจาก grade |
| **39** | `Pass`<br>`Finish` | score ไม่ผ่าน >=80 จึงข้ามไป else if |
| **40** | `Fail`<br>`Finish` | score < 50 เข้า final else |
| **41** | `Grade A`<br>`Low Attendance`<br>`Finish` | attendance 60 < 80 เข้า else ของ if ซ้อน |
| **42** | `15`<br>`20` | `x < y` เป็นจริง → `x = x + 5` |
| **43** | `40`<br>`35` | `x < y` เป็นเท็จ → `y = y + 5` |
| **44** | `15`<br>`20` | `15 < 15` เท็จ → เข้า else `y+5` |
| **45** | `13` | ❌ อัปเดตสองครั้ง: `5→10` แล้ว `10→13` |
| **46** | `18` | *(15 < 10) เท็จ* → ตรง `(15 > 5)` → `15+3=18` |
| **47** | `13` | `(10 < 10) เท็จ` → ตรง `(10 > 5)` → `10+3=13` |
| **48** | `Result = Pass` | assign string แล้ว println ต่อท้าย |
| **49** | `Result = Fail` | score 50 < 60 เข้า else |
| **50** | `A`<br>`End` | |
| **51** | `B`<br>`End` | `(30 <= 20) เท็จ` → เข้า else ข้างใน |
| **52** | `C`<br>`End` | `(5 >= 10) เท็จ` → เข้า else ข้างนอก |
| **53** | `A1`<br>`End` | score>=80 และ age>=18 ตรงทั้งคู่ |
| **54** | `A2`<br>`End` | scoreตรง แต่ age=15 < 18 เข้า else |
| **55** | `B1`<br>`End` | scoreไม่ผ่าน else if แรก → เช็ค age ได้ B1 |
| **56** | `C`<br>`End` | score<50 เข้า final else |
| **57** | `C`<br>`End` | เช่นเดียวกับข้อ 56 |
| **58** | `Tuesday` | match case 2 |
| **59** | `Other Day` | ไม่ตรง case เลยเข้า default |
| **60** | `Other Day` | 0 ไม่ตรง case เลยเข้า default |
| **61** | `Delete`<br>`End` | break หยุด switch แล้วพิมพ์ End ต่อ |
| **62** | `Invalid`<br>`End` | match default |
| **63** | `One` | มี break หยุดที่ case 1 |
| **64** | `Two`<br>`Three`<br>`Four` | ❌ **Fall-through**: ขาด break เลยรันต่อจนสุด case 4 |
| **65** | `Three`<br>`Four` | ❌ **Fall-through**: ขาด break เลยรันต่อจนสุด case 4 |
| **66** | `Other` | ไม่ตรง case → default |

---

## 📝 ส่วนที่ 2: เติมโปรแกรมส่วนที่ขาด (ข้อ 1–30)
> ✅ เรียงตามลำดับช่องว่างจากซ้ายบนลงล่างในแต่ละ block ของโจทย์

| ข้อ | โค้ดที่ต้องเติมในช่องว่าง | คำอธิบาย / จุดสังเกต |
|:---:|:--------------------------|:----------------------|
| **1** | `score >= 50`, `"Pass"` | เช็ครอบผ่านตามเงื่อนไข |
| **2** | `age >= 18`, `"Adult"` | เช็คอายุตามกฎหมาย |
| **3** | `number > 10`, `"Greater than 10"` | โจทย์ระบุว่า **"มากกว่า"** ใช้ `>` เท่านั้น (ไม่ใช่ `>=`) |
| **4** | `number % 2 == 0`, `"Even"`, `"Odd"` | เศษเป็น 0 คือคู่ else คี่ |
| **5** | `score >= 60`, `"Fail"` | else ต้องแสดงผลไม่ผ่าน |
| **6** | `age >= 18`, `"Can Vote"`, `"Cannot Vote"` | ⚠️ ใบงานเดิมพิมพ์ `int score` ผิด ให้แก้เป็น `age` ตามหัวข้อโจทย์ |
| **7** | `x > y`, `"y is greater"` | if ตรวจสอบว่า x มากกว่า else แสดงผล y |
| **8** | `number > 0`, `"Negative"` | ถ้ามากกว่า 0 เป็นบวก else เป็นลบ |
| **9** | `score >= 80`, `score >= 70`, `score >= 60`, `score >= 50` | ต้องเรียงจากมากไปน้อยใน else-if |
| **10** | `score >= 70`, `score >= 60` | เรียงลำดับเงื่อนไขตามเกณฑ์คะแนน |
| **11** | `age >= 18 && score >= 50` | ใช้ `&&` เพราะต้องตรงทั้งสองเงื่อนไข |
| **12** | `price >= 1000` | เช็ครอบส่วนลด |
| **13** | `score >= 50 && score <= 100` | เช็คช่วงค่า (Range check) |
| **14** | `age >= 18`, `score >= 50` | if ซ้อนตรวจสอบอายุก่อน แล้วค่อยเช็คคะแนน |
| **15** | `member == true`, `price >= 1000` | เช็คสมาชิกก่อน ถ้าใช่ค่อยเช็คยอดซื้อ |
| **16** | `menu`, `"Edit"` | ใส่ชื่อตัวแปรใน switch และข้อความ case 2 |
| **17** | `day`, `"Friday"` | ตามลำดับวันในโจทย์ |
| **18** | `price >= 5000`, `price >= 3000` | เรียงจากเงื่อนไขยากไปง่าย |
| **19** | `score >= 80 && attendance >= 80`, `attendance >= 80`, `score >= 50` | เช็คเกรด A ก่อน ถ้าไม่ผ่านเช็คเกรดอื่นต่อ |
| **20** | `price >= 5000`, `price * 0.20`, `price >= 3000`, `price * 0.10`, `0`, `price - discount` | คำนวณส่วนลดและราคาสุทธิตามโครงสร้าง if-else-if |
| **21** | `order >= 1000`, `order >= 500`, `order + shipping` | เช็คเงื่อนไขค่าส่งและคำนวณรวม |
| **22** | `age >= 18 && score >= 50 && gpa >= 2.00` | ⚠️ โจทย์ใบงานเดิมวางโค้ดผิด ให้เติมเงื่อนไขครบทั้ง 3 ตัวแปร |
| **23** | `(gpa >= 3.50 && income < 20000) || gpa >= 3.80` | ใช้ `||` เพื่อรวม 2 กลุ่มสิทธิ์ที่ผ่านได้ |
| **24** | `hasCard`, `lateMinute > 30` | เช็คบัตรก่อน ถ้ามีค่อยเช็คความสาย |
| **25** | `withdraw > 0`, `withdraw <= balance`, `balance - withdraw` | ป้องกันเงินถอนติดลบและเกินยอดจริง |
| **26** | `score >= 80`, `attendance >= 80`, `score >= 70`, `score >= 60` | เช็คเกรด A ก่อน (มีเช็ค attendance) แล้วไล่ downgrade |
| **27** | `member == true`, `price >= 3000`, `price >= 3000`, `price * discount`, `price - (price * discount)` | if ซ้อนเช็ครอบส่วนลดสมาชิก vs ไม่เป็นสมาชิก |
| **28** | `midterm + finalExam + assignment`, `total >= 80`, `total >= 70`, `total >= 60`, `total >= 50` | คำนวณรวมก่อน แล้วเช็คเกรดตามเกณฑ์ |
| **29** | `hour <= 1`, `hour <= 4`, `20 + (hour - 1) * 10`, `20 + 3 * 10 + (hour - 4) * 20` | สูตรคิดค่าจอดแบบ tiered pricing |
| **30** | `memberType.equals("Gold")`, `0.20`, `memberType.equals("Silver")`, `0.10`, `memberType.equals("Member")`, `0.05`, `0`, `price >= 5000`, `discountRate + 0.05`, `discountRate > 0.25`, `0.25`, `price * discountRate`, `price - (price * discountRate)` | เช็คสิทธิ์สมาชิก → เพิ่มส่วนลดเมื่อซื้อแพง → คัตสูงสุดที่ 25% → คำนวณผลสุดท้าย |

---

## 🔍 สรุปจุดที่หลุดในไฟล์เดิม & การแก้ไข
| กลุ่มข้อ | สาเหตุที่ผิด | วิธีแก้ / ความรู้ที่ต้องจำ |
|:---:|:---|:---|
| **7, 45** | ไม่ไล่ค่าตัวแปรจนบรรทัดสุดท้ายก่อนตอบ Output | ให้ทำ Table Trace: `Step → ตัวแปร → เงื่อนไข → คำสั่ง` จนจบ method |
| **64, 65** | ลืมว่า `switch-case` ขาด `break;` จะเกิด **Fall-through** | กรณีไม่มี break โปรแกรมจะพิมพ์ทุก case ที่ลงมาติดกันจนเจอ break หรือจบ switch |
| **3 (ส่วน 2)** | ใช้ `>=` ทั้งที่โจทย์ระบุชัดเจนว่า "มากกว่า" | `>` = มากกว่า, `>=` = มากกว่าหรือเท่ากับ ต้องอ่านคำบรรยายเงื่อนไขให้ตรง operator |
| **6, 22 (ส่วน 2)** | copy โค้ดจากข้อก่อนหน้ามาวางโดยไม่ได้แก้ตัวแปร/เงื่อนไขตาม header โจทย์ | siempre ตรวจสอบ `header` ตัวแปรและโจทย์ว่าถามเรื่องอะไร ก่อนเติมโค้ด |

✅ **ตรวจสอบครบถ้วนทุกข้อ 1–96 ตามหลักมาตรฐาน Java 8+**  
หากต้องการให้จัด format เป็นไฟล์ `.md` พร้อมลิงก์ดาวน์โหลด หรือต้องการคำอธิบายเชิงลึกเพิ่มเติมในข้อใด แจ้งได้เลยครับ! 🚀
